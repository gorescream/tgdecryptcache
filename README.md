# tgdecryptcache
decrypt telegram cache files.

use: dec -p [full path to key_data files] [user_data folder] or -u [username].
gui similarly.
console (basic dec.py) just unpack to folder. gui at first unpack to ram, you can choose which pictures save

for example: py dec.py -u User
